//to immport those functions
const math = require("./math");

const num1 = 8 , num2 = 3;

console.log("Addition of 2 numbers is:", math.addFn(num1, num2));
console.log("Subtraction of 2 numbers is:", math.subFn(num1, num2));